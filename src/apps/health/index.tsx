import { PlaceholderApp } from '../placeholder/PlaceholderApp'
import { Heart } from 'lucide-react'

export default function HealthApp() {
  return <PlaceholderApp name="Health" icon={Heart} color="#FF2D55" />
}
