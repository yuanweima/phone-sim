import { PlaceholderApp } from '../placeholder/PlaceholderApp'
import { AppWindow } from 'lucide-react'

export default function AppStoreApp() {
  return <PlaceholderApp name="App Store" icon={AppWindow} color="#007AFF" />
}
