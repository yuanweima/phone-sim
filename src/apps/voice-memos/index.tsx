import { PlaceholderApp } from '../placeholder/PlaceholderApp'
import { Mic } from 'lucide-react'

export default function VoiceMemosApp() {
  return <PlaceholderApp name="Voice Memos" icon={Mic} color="#EF4136" />
}
