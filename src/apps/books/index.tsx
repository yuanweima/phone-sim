import { PlaceholderApp } from '../placeholder/PlaceholderApp'
import { BookOpen } from 'lucide-react'

export default function BooksApp() {
  return <PlaceholderApp name="Books" icon={BookOpen} color="#FF9500" />
}
