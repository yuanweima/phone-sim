import { PlaceholderApp } from '../placeholder/PlaceholderApp'
import { Wallet } from 'lucide-react'

export default function WalletApp() {
  return <PlaceholderApp name="Wallet" icon={Wallet} color="#000000" />
}
