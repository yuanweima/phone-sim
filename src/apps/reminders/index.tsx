import { PlaceholderApp } from '../placeholder/PlaceholderApp'
import { Bell } from 'lucide-react'

export default function RemindersApp() {
  return <PlaceholderApp name="Reminders" icon={Bell} color="#FF9500" />
}
